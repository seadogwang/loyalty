<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loyalty.member.merge.infrastructure.MergeMapper">
    <update id="transferIdentities">
        UPDATE loyalty.member_identity SET member_id = #{targetMemberId}, updated_at = now()
        WHERE member_id = #{sourceMemberId} AND status = 'ACTIVE'
    </update>
    <insert id="insertMergeHistory">
        INSERT INTO loyalty.member_merge_history (tenant_id, program_id, source_member_id, target_member_id, reason, operator_id)
        VALUES (#{tenantId}, #{programId}, #{sourceMemberId}, #{targetMemberId}, #{reason}, #{operatorId})
    </insert>
    <insert id="insertCanonicalMapping">
        INSERT INTO loyalty.canonical_member_mapping (tenant_id, program_id, source_member_id, target_member_id)
        VALUES (#{tenantId}, #{programId}, #{sourceMemberId}, #{targetMemberId})
    </insert>
</mapper>
