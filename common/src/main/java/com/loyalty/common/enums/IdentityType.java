package com.loyalty.common.enums;

/** Identity type (design 4.6). EXTERNAL_ID must be namespaced by identity_source. */
public enum IdentityType {
    PHONE,
    EMAIL,
    MEMBER_CARD,
    APP_USER_ID,
    POS_ID,
    EXTERNAL_ID
}
