<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loyalty.engine.benefit.infrastructure.TierBenefitMappingViewMapper">
    <select id="findBenefitIdsByTier" resultType="java.util.UUID">
        SELECT benefit_id FROM loyalty.tier_benefit_mapping
        WHERE tenant_id = #{tenantId} AND program_id = #{programId} AND tier_id = #{tierId}
          AND (effective_to IS NULL OR effective_to > now())
    </select>
</mapper>
