<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loyalty.engine.point.infrastructure.PointAccountLockMapper">

    <insert id="upsert">
        INSERT INTO loyalty.point_account_lock (tenant_id, program_id, account_id, point_type_id)
        VALUES (#{tenantId}, #{programId}, #{accountId}, #{pointTypeId})
        ON CONFLICT (tenant_id, program_id, account_id, point_type_id) DO NOTHING
    </insert>

    <select id="lockForUpdate" resultType="int">
        SELECT version FROM loyalty.point_account_lock
        WHERE tenant_id = #{tenantId} AND program_id = #{programId}
          AND account_id = #{accountId} AND point_type_id = #{pointTypeId}
        FOR UPDATE
    </select>
</mapper>
