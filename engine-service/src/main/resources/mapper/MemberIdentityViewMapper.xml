<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loyalty.engine.point.infrastructure.MemberIdentityViewMapper">
    <select id="findMemberId" resultType="java.util.UUID">
        SELECT member_id FROM loyalty.member_identity
        WHERE program_id = #{programId}
          AND identity_type = #{type}
          AND identity_source = #{source}
          AND normalized_value = #{normalized}
          AND status = 'ACTIVE'
        LIMIT 1
    </select>
</mapper>
