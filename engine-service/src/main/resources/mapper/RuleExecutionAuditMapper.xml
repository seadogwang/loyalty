<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loyalty.engine.point.rule.infrastructure.RuleExecutionAuditMapper">

    <insert id="insert">
        INSERT INTO loyalty.rule_execution_audit
          (id, tenant_id, program_id, rule_definition_id, rule_version_id, correlation_id,
           input_snapshot, result_snapshot, status, execution_ms)
        VALUES (#{id}, #{tenantId}, #{programId}, #{ruleDefinitionId}, #{ruleVersionId},
                #{correlationId}, (#{inputSnapshot})::jsonb, (#{resultSnapshot})::jsonb,
                #{status}, #{executionMs})
    </insert>
</mapper>
